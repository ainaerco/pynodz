# Icon Migration - Deferred Items

## Keep as PNG (not migrated)

- **Grid background** (`resources/icons/grid.png`): Canvas background
- **Transparency pattern** (`resources/icons/transparent_small.png`): RGBA picker checkerboard

## Notes

- Qt Awesome prefixes: `fa6`, `fa6s`, `fa6b`, `mdi`, `ph`, `ri`, `msc`
- Run icon browser: `uv run qta-browser`
- Full icon list: https://fontawesome.com/icons
- `getIcon()` still works for custom user icons (NodeBookmark)
